# 主题优先研究工作台测试记录

## 自动化验证

- 研究工作台专项：`188 passed`。
- Ruff：`make lint` 通过。
- 补丁检查：`git diff --check` 通过。
- 全仓回归：功能分支 `653 passed / 141 failed`；`main` 基线 `589 passed / 152 failed`。
- 失败集合对比：功能分支新增失败 `0`，已有失败减少 `11`。

全仓剩余失败集中在旧版 Web 页面契约和既有每日流水线环境用例。本次默认原生研究工作台的专项、API、快照、供应商客户端和每日研究任务均通过。

## 真实数据验证

执行：

```bash
PYTHONPATH=src python scripts/run_daily_research.py \
  --output-dir reports/research \
  --refresh
```

结果：

- 大盘：3 个分区，包含 5 个主题、5 项真实涨跌分布和 10 只热门股。
- 持仓：11 只持仓全部进入研究；主题按独立持仓数聚合，分化只展示至少 2 只持仓的可比主题。
- 个股：`603278` 返回 8 个维度槽位，结论为人话状态、原因和下一步。
- 机会：5 个主题、10 只候选，候选按命中的主线主题分组。

## 浏览器验证

- `1280 x 900`：结论、主题、涨跌分布和热门股进入桌面首屏；页面无横向溢出。
- `390 x 844`：结论、行动、风险和主题首卡进入首屏；根宽等于视口，只有主题带内部横向滚动。
- 移动输入框为 `16px`，核心决策与状态标签不小于 `10px`。
- 浏览器控制台无错误或警告。
- 页面与公开结果未出现研究能力供应商品牌、伪分类或真实凭证。

## 安全边界

- 持仓研究仅提交代码、名称和已有主题字段，不提交股数、成本、权重或账号身份。
- 页面动态结果使用 DOM 节点和 `textContent`，不使用 `innerHTML`。
- 结论不包含收益承诺、自动交易或无条件买卖语言。

## 公网验证

- `https://stock.jiewat-kaka-fj.com/healthz` 返回 200。
- 登录后四模块 endpoint 均返回 200。
- 大盘与机会读取最新共享快照；持仓与个股在外部额度耗尽时明确降级为“待确认”。
- `stock-ts.service` 和 `stock-ts-daily-research.timer` 均为 active。
