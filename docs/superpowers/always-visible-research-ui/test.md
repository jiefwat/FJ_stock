# 验证记录

## 自动化

- Python 3.12 专项：`161 passed in 8.57s`。
- Ruff：`All checks passed`。
- Legacy Web 回滚：`266 passed in 12.12s`。
- 全仓默认模式：`664 passed / 141 failed`；失败数和失败集合与改造前基线一致，本次新增测试全部通过。
- `git diff --check`：通过。

专项覆盖：

- 个股八维本地证据、缺失维度和候选池 K 线降级。
- 候选没有独立个股快照时复用机会快照，保留真实主题、依据和失效条件。
- 持仓全部标的、主题暴露、主题内分化和前三优先处理。
- 大盘宽度、指数、主题和热门股。
- 热点机会的主线、候选、确认和淘汰条件。
- `fresh snapshot -> live -> stale snapshot -> local fallback` 交付顺序。
- 登录账号持仓路径服务端注入，浏览器持仓路径无效。
- 本地证据状态、刷新失败保留、纯文本 DOM 渲染和移动端布局。

## 真实额度耗尽

使用本机安全配置和真实 TDX 快照调用四个产品 API，未输出密钥或原始错误：

| 模块 | HTTP | 交付 | 发现 | 模块项 | 专属分区 |
| --- | ---: | --- | ---: | ---: | ---: |
| 每日大盘 | 200 | 历史参考 | 3 | 3 | 3 |
| 我的持仓 | 200 | 本地证据 | 3 | 11 | 2 |
| 个股分析 | 200 | 本地证据 | 3 | 8 | 0 |
| 热点机会 | 200 | 历史参考 | 3 | 10 | 2 |

四个响应均有非空结论，且未出现供应商品牌、能力 ID、trace、网关或凭证字段。

## 浏览器

- `1280x900`：大盘、持仓、个股、机会均生成内容；持仓显示 11 只、2 个专属分区；个股显示 8 维和 3 条依据；页面 `scrollWidth=1280`。
- `390x844`：页面 `scrollWidth=390`，无横向溢出；四个移动导航按钮高度均为 `44px`。
- 交互：模块切换、快速个股搜索、机会候选下钻、完整依据展开和 `Escape` 关闭均通过。
- 候选下钻：只有机会快照、没有独立个股快照的东山精密仍显示 3 条依据和 8 维覆盖，不再进入空白状态。
- 刷新失败：可执行 DOM 测试确认保留刷新前结论，只显示“刷新失败，已保留现有内容”。
- 浏览器控制台 warning/error：`0`。

## 已知全仓基线

默认原生页面模式仍有 141 项既有失败：136 项旧页面断言与原生四工作台目标冲突，5 项为既有日报流水线测试。Legacy Web 独立回滚集全绿，本次没有增加失败集合。

## 公网

- `https://stock.jiewat-kaka-fj.com/healthz`：HTTP 200。
- 根路径：HTTP 303，跳转登录页；登录页 HTTP 200。
- 登录态四模块 API：全部 HTTP 200；持仓 11 项、个股 8 维、机会 10 项，四页均有 3 条发现。
- `stock-ts.service`、`stock-ts-daily-research.timer`、`stock-ts-daily-analysis.timer`：均为 `active`。
- GitHub 独立分支、本地独立分支和 `/opt/stock-ts` 提交一致；`main` 保持在改造前基线。
