# 可用研究决策验证记录

## 真实查询校准

校准时间：2026-07-14。调用只使用服务端固定能力白名单，凭证从本机忽略提交的 `.env` 读取；命令只打印能力名、行数和字段名，不打印环境值、请求头或密钥。

| 模块 | 维度 | 行数 | 语义结果 |
| --- | --- | ---: | --- |
| 每日大盘 | 指数 | 3 | 上证指数、深证成指、创业板指；含点位、涨跌幅、成交额 |
| 每日大盘 | 宏观 | 1 | 仅返回指数报价，未达到宏观证据门禁，标记 `insufficient` |
| 每日大盘 | 行业方向 | 5 | 含行业名称、板块热度、成交额、成分领域 |
| 每日大盘 | 新闻 | 10 | 含标题、摘要、发布日期和公开链接 |
| 热点机会 | 概念方向 | 5 | 含概念名称、板块热度和成交额 |
| 热点机会 | A 股候选 | 10 | 含股票身份、收入/利润变化、成交额、成交量和换手率 |
| 热点机会 | 事件 | 10 | 含报告期和部分业绩预告变化；只有身份/报价时由门禁排除 |
| 热点机会 | 新闻 | 10 | 含标题、摘要、发布日期和公开链接 |

板块查询对比：

- `申万一级行业 按成交额和热度排序 前5`：0 行，不采用。
- `行业板块 排除融资融券 按成交额和热度排序 前5`：5 行，返回半导体、通信设备等可解释行业，作为大盘查询。
- `概念板块 排除融资融券 按板块热度排序 前5`：5 行，返回创新药、PCB、CPO、存储芯片、商业航天，作为机会查询。

## 真实个股与持仓字段

验证对象：`603278 大业股份`。

| 模块 | 维度 | 行数 | 核心字段 |
| --- | --- | ---: | --- |
| 个股 | 财务 | 1 | 2023-2025 营业收入、归母净利润、经营现金流、ROE、资产负债率及同比 |
| 个股 | 经营 | 1 | 主营产品、经营范围、竞争对手 |
| 个股 | 机构预期 | 1 | 2026、2027 预测净利润中值 |
| 个股 | 事件 | 1 | 2026 一季度收入、利润及同比 |
| 持仓 | 事件 | 1 | 半年度业绩预告上下限、变化类型、原因和摘要 |
| 持仓 | 公告 | 10 | 标题、摘要、发布日期和公开链接 |
| 持仓 | 机构预期 | 1 | 只有身份和报价，标记 `insufficient` |
| 持仓 | 行情 | 1 | 近期收盘价、成交量、成交额、振幅和换手率 |

## 产品结果

- `stock`：`complete`。首屏依次显示最新事件、财务方向、机构预期；财务结论能识别收入回落以及利润和经营现金流同时为负。
- `market`：`partial`。指数、行业、新闻可用；宏观返回内容不符合宏观语义，明确列入缺口。
- `portfolio`：`partial`。事件、公告、行情可用；身份/报价型机构预期不再冒充完整证据。
- `opportunity`：`complete`。概念方向、A 股候选、业绩事件和新闻均通过语义门禁。
- 四个产品响应均未出现供应商品牌、能力 id、trace、请求头、API key 或网关字段。

## 自动化红绿证据

- 首个证据测试因 `stock_ts.research_evidence` 不存在而收集失败，随后实现能力级字段提取。
- 身份型机构预期测试先得到 `ready`，接入语义门禁后变为 `insufficient`。
- 个股卡片测试先以股票代码、名称、最新价和涨跌幅开头，修复后改为事件、财务和预期事实。
- 查询合同测试先证明大盘和机会使用宽泛自然语言，校准后固定指数、排序、结果数和可解析筛选条件。
- 真实数据复核发现增长率被误标为金额、新闻先显示时间/URL、财务同比被当作收入期间以及旧财务风险优先于最新事件；每项均先增加失败测试再修复。

## 最终回归

- 专项研究/API/UI/安全：`104 passed in 1.83s`。
- 默认全仓：`569 passed / 141 failed`；失败数与改造前一致，为旧页面断言和日报日期基线。
- 强制 legacy：`701 passed / 9 failed`；5 项日报日期基线，4 项为 native 页面专测与强制 legacy 模式冲突。
- `make lint` 和 `git diff --check` 均通过。
- 1280px：`scrollWidth=1280`；大盘覆盖度 3/4，新闻摘要不超过 80 字。
- 390px：`scrollWidth=390`、工作台宽 366px、卡片宽 310px；个股覆盖度 4/4；浏览器控制台无 warning/error。
