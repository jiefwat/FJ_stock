# Handoff

## 当前状态

实现、专项验证、浏览器检查和代码审查已完成；尚未推送、合并或部署。

## 工作分支

`codex/stock-method-ui-v2`

## 安全边界

- 不修改 `data/`、`reports/`、持仓、凭证或服务器配置。
- stale 数据硬闸门和条件化动作边界必须保留。

## 主要完成项

- 新增 `ThesisFramework` 与 `WeightedEvidence`。
- 形成研究假设、核心矛盾、三段因果链、五类证据权重和事实/推断/未知分层。
- 将财务趋势、事件风险、估值约束、价格确认和仓位边界贯通。
- 页面重排为投资判断、决策条件、执行边界、论点链、关键证据、风险反证、三情景和证据账本。
- 删除多角色方法旁白，保留原始分析和来源审计。
- 完成 1280px 与 390px 本地浏览器验证。

## 验证

- Ruff：通过。
- 个股模型与页面专项：`33 passed`。
- Web 与专业研究回归：`223 passed`。
- 全仓：`571 passed, 5 failed, 10 warnings`；5 个失败均为既有 `tests/test_daily_pipeline.py`。

## 本地预览

`http://127.0.0.1:8517/?code=603278&provider=tdx-snapshot#stock`

## 下一步

根据用户指示决定是否推送分支、合并 `main`、部署服务器并刷新生产数据。
