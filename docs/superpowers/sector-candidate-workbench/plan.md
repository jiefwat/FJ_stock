# Plan

1. 用测试锁定板块分析和候选观察池输出结构。
2. 扩展模型：SectorSnapshot、SectorAnalysisReport、CandidateStock、CandidatePoolReport。
3. 扩展 provider：sample 支持板块列表和候选股票池。
4. 实现评分：趋势、量价、板块、资金、风险扣分。
5. 扩展 report：板块 Markdown、候选 Markdown、完整日报拼接。
6. 扩展 CLI：sectors、candidates，daily 同步包含新增章节。
7. 扩展 web.py 页面卡片。
8. 运行 lint、pytest、CLI、页面验证。
