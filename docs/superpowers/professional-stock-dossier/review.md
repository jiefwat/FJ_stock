# Review

审查日期：2026-07-13

## Findings

审查后未保留 P0/P1 finding。审查中发现并已修复：

- P1：持仓成本为 0 时计算相对成本会除零；现改为显示“成本待补录”，并回归覆盖。
- P1：行情过期时五步轨道仍显示旧价格触发；现全部切换为暂停/刷新后重算，价格相关证据同步标记 stale。
- P2：事件反证按输入顺序而非严重度排序；现按 critical、high、medium、low 排序。
- P2：“解除质押”被关键词规则误判为高风险质押；现识别解除语义，不生成高风险项。
- P2：390px 下数据中台摘要逐字换行；现改为移动端三行网格，并验证无页面级横向溢出。

## Assumptions

- 核心结论由确定性规则与可追溯证据生成，不引入 LLM、目标价或未经校准的上涨概率。
- 公告标题与摘要只做风险筛查，页面明确要求复核原文。
- 仓位上限是研究风控边界，不是自动交易指令。

## Residual Risks

- 事件分类仍是规则筛查，复杂否定、监管语义和公告原文影响需要人工复核。
- 行业相对估值与多期财务数据仍受上游数据覆盖限制；缺失时系统主动降级，不输出低估结论。
- 全量套件保留 6 个既有基线失败，详见 `test.md`。

Decision: 可合并；本需求范围内未发现未修复的高优先级问题。
