# Handoff

状态：本地实现与质量验证完成，待合并、推送与部署。

## 交付内容

- 新增确定性的 `ProfessionalStockDossier` 聚合模型与财务、估值、技术、资金、事件诊断。
- 个股首屏只保留一个投委会结论，随后依次展示五步决策轨道、风险登记表、仓位边界、诊断底稿和三种情景。
- 真实 `603278` 能明确暴露亏损、负 PE 失效、PB 口径冲突、65.72% 股权质押和陈旧行情硬停。
- 已持仓与未持仓使用不同执行文案；成本只用于风险管理，不作为看多证据。
- 旧 TradingAgents/深度诊断保留在折叠支持层，不再与主结论竞争。

## 运维边界

- 部署时只同步 Git 跟踪源码；保留服务器 `.env`、`.secrets`、真实快照、账户持仓、报告、Nginx、systemd timer/service 与独立 DSA。
- 部署完成后必须核对服务器 HEAD、GitHub `main` 和本地 `main` 一致，并验证登录页与受保护个股页。

## 已知事项

- 全量 pytest 为 519 passed / 6 baseline failed；不得描述为“全量全绿”。
- 公告筛查不替代公告原文和正式投资研究。
