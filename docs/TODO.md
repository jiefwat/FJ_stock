# TODO

## 当前已完成

- [x] Web 增加系统状态模块，展示 provider、持仓路径、缓存目录和 token 配置状态。
- [x] `daily` 支持 `--cache-dir` 与 `--refresh`，可复用日报缓存。
- [x] 增加 `doctor` 运行体检命令，输出 sample 主流程、依赖和 token 安全摘要。
- [x] 抽出 `workflows.py` 服务编排层，CLI/Web 复用同一主流程。
- [x] 增加 Web 错误页，数据源或持仓文件失败时给出可读提示。
- [x] 增加轻量 JSON 缓存工具，为后续 SQLite/Parquet 缓存铺路。
- [x] 增加本地行情 CSV 导入，可直接生成个股分析。
- [x] 增加新闻舆情 CSV 导入，并可嵌入完整每日复盘。
- [x] 增加 `send-daily`，支持邮件、企业微信、飞书外发及 dry-run / 单渠道测试验证。
- [x] 增加交易流水 CSV，可按移动加权成本生成当前持仓后做组合分析。
- [x] AKShare 大盘在全市场 spot 接口失败时降级为指数可用，避免真实大盘报告直接崩溃。
- [x] AKShare 增加真实行业板块、候选池和个股新闻抓取能力。
- [x] 优化完整日报，增加今日一句话、最需要关注的 3 件事、持仓风险 Top 3。
- [x] 增加 `stock-deep`，支持单股多角度深度分析、未来上涨潜力观察分和多头/空头/裁判对抗结论。
- [x] 增加 `batch`，支持多股票批量深度分析和观察优先级排序。
- [x] 增加 `daily-deep`，支持大盘、板块、持仓、新闻、候选池和个股对抗摘要的每日深度复盘。
- [x] 增加单文件 HTML 结论页，支持单股、批量和每日深度复盘输出。
- [x] 增加 `watchlist` 自选股研究工作台，支持研究假设、标签、价格/评分提醒和深度排序。
- [x] 增加 `backtest` 轻量均线回测，支持本地行情 CSV 的收益、回撤、胜率和交易明细输出。
- [x] 增加 `ai-insight` 可选大模型增强研报；无 Key 自动降级，有 Key 时调用 OpenAI-compatible / 千问 DashScope 兼容接口。
- [x] 增加股票名称解析与腾讯行情源，Web 默认 `auto` 不再把中文名直接当作 sample 代码，并显示数据质量、样例/降级和行情日期告警。
- [x] Web 个股模块升级为多角度深度分析，展示未来上涨潜力观察分、多轮对抗、情景推演、风险与失效条件。
- [x] Web 全局模块专业化：新增专业投研框架、大盘情景推演/风险闸门、板块主线确认矩阵、组合风险预算/持仓处理剧本、候选池评分方法/分层和能力覆盖矩阵。
- [x] 吸收 GitHub 优秀股票项目思路：落地 AI 决策仪表盘、策略透镜矩阵、研究团队分工、数据块完整性、观察点位、催化/风险和交易纪律。
- [x] 增加多源数据矩阵和 `tdx-snapshot` provider，支持把 TDX MCP/通达信服务导出的本地 JSON 快照纳入项目分析。
- [x] 增加 iTick 可选报价/K线补强，配置 `ITICK_API_KEY` 后由快照脚本写入本地 JSON。
- [x] 增加 `eltdx` 外部桥接 provider，使用 `python3.11` 访问最新 quote、日线和主题数据，并让 `auto` 在可用时优先走这条链路。
- [x] 增加 `announcements` 公告/财报事件命令，支持从 CNInfo/巨潮抓取公告并识别减持、监管、诉讼、质押等标题风险标签。
- [x] 增加专业单股研究包与 Web 展示，整合盘口技术结构、支撑/压力/失效线、量能比、公告事件雷达和复核动作。
- [x] Web 增加研究指挥舱 UI，将研究偏向、观察分、市场热度、技术红线、候选首位、证据链、下一步动作、风险红线和数据可信度聚合到首页决策区。
- [x] 增加明确操作计划模块，输出当前动作、目标仓位、买入/加仓触发、止损/减仓触发、止盈计划、禁止动作和盘中执行清单。
- [x] 增加组合整体建议模块，展示持仓添加位置，并给出总体动作、目标现金仓位、每只持仓目标仓位、调整金额、止损和止盈观察。
- [x] Web 账号体系升级为“共享行情能力 + 按账号隔离持仓”：登录用户自动使用专属持仓账本，页面参数不能覆盖其他用户账本。
- [x] 增加自动日报产物脚本 `scripts/run_daily_analysis.py`，生成 `reports/daily/latest.md`、日期归档、`latest.status` 和 HTML 归档。
- [x] 增加完整每日数据流水线 `scripts/run_daily_pipeline.py`，串联 TDX 全市场刷新、候选 K 线/主题补强、多源 K 线/资金/估值/新闻补强、公告摘要和日报生成。
- [x] Web 每日复盘模块读取最新自动日报产物，展示生成状态、摘要、持仓要点和 HTML 产物路径。
- [x] 增加结构化日报决策产物 `reports/daily/latest_decisions.json`，沉淀大盘摘要、红黄绿持仓、今日机会、数据限制、自动任务异常、每只持仓触发线/止损线/可信度；晨报和首页优先读取 JSON，减少 Markdown 解析误差。
- [x] Web 从长 HTML 报告改为应用式模块切换壳，新增顶部应用工具栏、左侧按钮导航和 hash 模块路由。
- [x] 首页增加用户工作台：按“我今天先看什么”组织组合风险、个股执行、候选机会和数据核对，并提供模块快速跳转。
- [x] Web 壳升级为任务式导航，增加全局数据状态条、侧边栏快速个股搜索和一键复制今日行动，减少每页重复说明。
- [x] Web 数据质量页增加自动更新监控，读取 `pipeline.status` 展示最近运行、每 2 小时节奏、失败步骤和处理建议。
- [x] Web 个股分析升级为交易快照 + 6 维判断 + 3 个风险：技术面、基本面、资金面、消息/公告、概念板块、成本位置，并前置一句最终动作。
- [x] Web 个股分析升级为投委会式专业研究档案：统一结论、五步决策轨道、风险登记表、财务/估值/技术/资金/事件诊断、条件化仓位边界和证据账本。
- [x] Web 设置页前置“系统健康检查”，自选研究标记为低频跟踪，减少低频模块对每日交易流程的干扰。
- [x] 参考 Vibe-Trading / Vibe-Research / Longbridge 方向新增研究运行卡：把目标、证据、动作、数据闸门和 Shadow Account 边界统一接入早间邮件与每日深度复盘。
- [x] 参考 TradingAgents 重构个股多轮对抗：从三段式多空裁判升级为技术、基本面、新闻情绪、多空研究员、交易员、风控经理、组合经理的角色链。
- [x] 继续参考 daily_stock_analysis：新增多源数据质量摘要、实时 quote fallback 骨架、fail-open 新闻情报源和新闻风险/催化分类。
- [x] 将 daily_stock_analysis 的上下文包/信号归因与 TradingAgents 的分析师团队、研究辩论、交易员、风控和组合经理流程真正接入个股页与 Markdown 个股报告，输出股票特异化证据和缺口降级。
- [x] 安装并读取 `yqn-dev-skills`。
- [x] 使用 `python-superpower-bootstrap` 生成 standard 级 Python 项目骨架。
- [x] 建立大盘分析、个股分析、报告渲染、CLI、sample provider。
- [x] 预留 AKShare provider 与 Streamlit Dashboard。
- [x] 补齐 pytest 回归测试。

## 自动执行 TODO

- 详细执行清单见 `docs/agent-ops/autonomous-product-todo.md`。后续 Agent 默认可直接实现其中事项，不需要逐项等待用户确认；完成后必须更新勾选状态、补测试并部署。

## 下一阶段优先级

1. 数据缓存：新增 SQLite/Parquet 缓存，避免重复拉取全市场数据。
2. 真实数据源增强：补 Tushare 真实财务、资金流和交易日历，并继续校准 Tencent / AKShare 字段口径。
3. 实时情报增强：把 `STOCK_TS_INTELLIGENCE_URLS` 接入稳定 NewsNow/RSS bridge，增加 SQLite 新闻缓存和源熔断。
3. 指标增强：补成交额分位、真实行业强弱排名、K 线图和历史评分回放。
4. 回测增强：补手续费、滑点、止损止盈、持仓天数、资金曲线导出和多策略对比。
5. 自选股增强：补 watchlist 分组、历史观察分趋势、提醒外发和每日自动跟踪。
6. 日报自动化：持续观察 `stock-ts-daily-analysis.timer` 的 `pipeline.status`，把失败项展示到 Web 数据质量模块。
7. AI 分析增强：将 `ai-insight` 扩展到 `daily-deep` 和 `watchlist`，并补结构化 JSON 输出、Prompt 回放测试和成本统计。
8. 决策产物增强：继续让每日复盘和个股页更多读取 `latest_decisions.json`，并把触发线、失效线和数据可信度细化到价格级。

## 风险与约束

- 外部行情接口可能变更字段或限频，真实数据源必须有集成测试和降级策略。
- 股票分析不能输出确定性收益承诺或无风险买卖建议。
- Tushare token、内部接口、任何真实密钥只能放本地环境变量，不能提交。
